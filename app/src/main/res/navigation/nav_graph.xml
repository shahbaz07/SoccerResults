<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/soccerResults">
    <fragment
        android:id="@+id/soccerResults"
        android:name="com.soccer.results.ui.SoccerResultsFragment"
        android:label="SoccerResults"
        tools:layout="@layout/soccer_results_fragment">
        <action
            android:id="@+id/action_navigate_to_detail"
            app:destination="@id/soccerResultDetail" />
    </fragment>
    <fragment
        android:id="@+id/soccerResultDetail"
        android:name="com.soccer.results.ui.SoccerResultDetailFragment"
        android:label="SoccerResultDetail"
        tools:layout="@layout/soccer_result_detail_fragment" >
    </fragment>
</navigation>